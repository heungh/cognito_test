# ===========================================
# Amazon Cognito 프로비저닝 설정 파일
# ===========================================
# 이 파일을 복사하여 config.env로 저장하고 값을 수정하세요
# cp config.env.example config.env
#
# 주의: config.env 파일은 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다.
# ===========================================

# -------------------------------------------
# AWS 기본 설정
# -------------------------------------------
AWS_REGION=ap-northeast-2
AWS_ACCOUNT_ID=your-account-id

# -------------------------------------------
# User Pool 설정
# -------------------------------------------
# User Pool 이름 (원하는 이름으로 변경)
USER_POOL_NAME=YourProject-UserPool

# Cognito Domain 접두사 (전역적으로 고유해야 함)
# 예: mycompany-auth → mycompany-auth.auth.ap-northeast-2.amazoncognito.com
DOMAIN_PREFIX=your-unique-domain-prefix

# -------------------------------------------
# 회원가입 허용 도메인 (보안 - 필수 설정)
# -------------------------------------------
# ⚠️ 중요: 이 설정이 없으면 모든 회원가입이 차단됩니다!
# Pre-Sign-Up Lambda가 이 도메인 목록을 검증합니다.
# 허용할 이메일 도메인을 콤마로 구분하여 입력하세요.
ALLOWED_SIGNUP_DOMAINS=your-company.co.kr,partner-agency.com,advertiser.co.kr

# -------------------------------------------
# 내부 사용자 판별 기준
# -------------------------------------------
# 내부 사용자로 판별할 이메일 도메인 (콤마로 구분)
# 이 도메인의 사용자는 자동으로 내부 사용자로 분류되어 즉시 승인됩니다.
INTERNAL_EMAIL_DOMAINS=your-company.co.kr,your-company.com

# 내부 사용자 사번 접두사 (콤마로 구분)
EMPLOYEE_ID_PREFIX=EMP,ADM

# -------------------------------------------
# App Client Callback URLs
# -------------------------------------------
# 웹 앱 콜백 URL (콤마로 구분)
WEB_CALLBACK_URLS=https://your-webapp.example.com/callback,http://localhost:3000/callback
WEB_LOGOUT_URLS=https://your-webapp.example.com/logout,http://localhost:3000/logout

# 모바일 앱 콜백 URL (콤마로 구분)
MOBILE_CALLBACK_URLS=https://your-mobile.example.com/callback,yourapp://callback
MOBILE_LOGOUT_URLS=https://your-mobile.example.com/logout,yourapp://logout

# -------------------------------------------
# Google OAuth (소셜 로그인용) - 선택사항
# -------------------------------------------
# Google Cloud Console에서 생성한 OAuth 클라이언트 정보
# https://console.cloud.google.com/ > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# -------------------------------------------
# 테스트 사용자 설정
# -------------------------------------------
# 테스트 사용자 임시 비밀번호 (첫 로그인 시 변경 필요)
TEST_USER_TEMP_PASSWORD=TempPass1!

# 테스트 내부 사용자 이메일
TEST_INTERNAL_USER_EMAIL=internal.user@your-company.co.kr
TEST_INTERNAL_USER_NAME=홍길동

# 테스트 관리자 이메일
TEST_ADMIN_EMAIL=admin@your-company.co.kr
TEST_ADMIN_NAME=관리자

# 테스트 외부 사용자 (광고주) 이메일
TEST_EXTERNAL_ADVERTISER_EMAIL=advertiser@external-company.com
TEST_EXTERNAL_ADVERTISER_NAME=김광고
TEST_EXTERNAL_ADVERTISER_COMPANY=ABC광고

# 테스트 외부 사용자 (대행사) 이메일
TEST_EXTERNAL_AGENCY_EMAIL=agency@external-agency.com
TEST_EXTERNAL_AGENCY_NAME=박대행
TEST_EXTERNAL_AGENCY_COMPANY=XYZ대행사

# -------------------------------------------
# Lambda 설정
# -------------------------------------------
# Lambda 함수 이름 접두사
LAMBDA_PREFIX=YourProject

# Lambda IAM 역할 이름
LAMBDA_ROLE_NAME=YourProject-CognitoLambdaRole

# -------------------------------------------
# Resource Server 설정
# -------------------------------------------
# Resource Server 식별자 (API 이름)
RESOURCE_SERVER_IDENTIFIER=your-api

# -------------------------------------------
# 아래는 프로비저닝 스크립트가 자동으로 채우는 값입니다.
# 직접 수정하지 마세요.
# -------------------------------------------
# USER_POOL_ID=
# USER_POOL_ARN=
# COGNITO_DOMAIN=
# WEB_CLIENT_ID=
# WEB_CLIENT_SECRET=
# MOBILE_CLIENT_ID=
# SERVER_CLIENT_ID=
# SERVER_CLIENT_SECRET=
